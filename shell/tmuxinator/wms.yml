# ~/.tmuxinator/wms.yml

name: wms
root: /fs/var/www/wms


windows:
  - editor:
      layout: main-vertical
      panes:
        - ls -la
        - tail -f protected/runtime/sql.log
        - tail -f protected/runtime/application.log

